extends layout 

block content 
    h1 Warning! This action cannot be undone 
    h2="Are you sure that you want to delete this title?:"
    h3= "'"+book.title+"'"
    form(method="post")
        input(type="submit" class="button" value="Delete book") 
    p 
        a(class="button" href="/books/") Cancel